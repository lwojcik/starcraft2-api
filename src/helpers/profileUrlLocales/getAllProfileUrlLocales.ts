import constants from '../../constants';

/**
 * Returns a list of all profile URL locales (e.g. 'en-us')
 *
 * @return List of all available profile URL locales as array of strings.
 */
export default () => constants.PROFILE_URL_LOCALES;
